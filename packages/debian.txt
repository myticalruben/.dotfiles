# Debian 13 (trixie) or sid. Hyprland is too new for stable/bookworm.
#
# UNVERIFIED: these names were derived from the Ubuntu list, which is mostly
# accurate because Ubuntu inherits Debian's naming, but nobody has run them on
# a Debian box yet. Check before trusting, on the target machine:
#
#     ./install.sh --verify-names
#
# That prints every name here that apt cannot resolve, without installing
# anything. Fix this file from its output and the guesswork is gone.
#
# Not covered here - see manual.md: rofi-wayland, quickshell, awww/swww,
# hyprshot, neovim, brave-browser.

hyprland
hyprlock
xdg-desktop-portal-hyprland
waybar
dunst
wlogout
cliphist
wl-clipboard
grim
slurp
jq
kitty
thunar
alacritty
pulseaudio-utils
wireplumber
playerctl
brightnessctl
network-manager-gnome
pavucontrol
polkit-kde-agent-1
btop
imagemagick
fonts-jetbrains-mono
